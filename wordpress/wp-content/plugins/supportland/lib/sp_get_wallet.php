<?php
    require_once 'sp_api.php';
 
    $my_user = new SP_User();
    $my_trans = new SP_Transaction($sp_user);
    /*
    if($sp_user->logged_in() == true)
        die( $sp_trans->get_wallet());
    else
        die(false);
*/






?>
